A library for generating text from *deterministic* probabilistic pushdown automata (PPDA).

to start run:
```bash
$ git clone git@github.com:franznowak/deterministic-ppda.git
$ cd deterministic-ppda
$ pip install -e .
```
